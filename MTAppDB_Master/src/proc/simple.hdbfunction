FUNCTION "SIMPLE_RV" (
IN QUANTITY INTEGER,IN ORDER_AMOUNT DECIMAL(10,2)) 
RETURNS TABLE ( "QUANTITY" INTEGER, "ORDER_AMOUNT" DECIMAL(10,2), "DISCOUNT" DECIMAL(3,2)
) LANGUAGE SQLSCRIPT
SQL SECURITY INVOKER AS 
BEGIN
CALL "SIMPLE_Proc" (
:QUANTITY,:ORDER_AMOUNT,returned_tt);
RETURN :returned_tt;
END;